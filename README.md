# DFAM Pro-Matrix Web

**Moog DFAM** の野性的なアナログサウンドと、**デジタルならではの高度なシーケンス・モジュレーション機能**を融合させた、プロフェッショナル仕様のWebベース音楽制作アプリケーションです。

Androidスマートフォン（横画面）での操作に特化し、スクロールのない没入感のあるUIと、PWAによるネイティブアプリのような体験を提供します。

## 🎹 Project Overview

このプロジェクトは、アナログ・セミモジュラー・シンセサイザーの名機からインスパイアを受けつつ、物理的な制約（パッチケーブルの煩雑さ、ステップ数の制限）をWebテクノロジーで解決することを目的としています。

### Core Philosophy
* **Zero Scroll / Single Screen:** 演奏中に画面をスクロールさせず、全てのパラメータに瞬時にアクセス可能。
* **Controlled Chaos:** 偶然性を味方につけるランダマイズ機能と、それを音楽的に制御する仕組みの両立。
* **Total Recall:** ブラウザを閉じても、リロードしても、直前の状態が失われない堅牢なデータ保存。

## ✨ Key Features

### 1. Main Performance (Tab 1)
* **Synth Engine:** Tone.js + Web Audio APIによる、太く有機的なアナログモデリング音源。
* **A/B Polyrhythmic Sequencer:** AパターンとBパターンを独立したサイクル数（1-8回）で切り替えることで、無限に変化するポリリズムを生成。
* **Groove & Modifiers:** 時間軸を歪ませるSwing/Groove機能と、ステップ再生順序をリアルタイムに入れ替えるSwap機能。

### 2. Digital Matrix (Tab 2)
* 物理ケーブルを廃した**ピン・マトリクス方式**のモジュレーション。
* 視認性の高いグリッドUIで、複雑なルーティング（FM、フィルター変調など）を一目で把握。
* 密度調整可能な「Smart Randomizer」を搭載。

### 3. Metro Editor (Tab 3)
* **Intellijel Metropolix Style:** 各ステップのパルスカウント（長さ）、ゲートタイプ、連符（Subdiv/Ratchet）を詳細に編集可能。
* **x2.5 Tuplets:** デジタルならではの「割り切れない連符」や複雑なSubdivisionに対応。

### 4. Library & System (Tab 4)
* **Total State Management:** 全設定をJSON形式でエクスポート/インポート。
* **PWA Support:** オフライン動作対応、ホーム画面へのインストール機能。
* **Auto-Save:** IndexedDBを使用した操作ごとのリアルタイム保存。

## 🛠 Tech Stack

* **Frontend:** React (TypeScript), Vite
* **Styling:** Tailwind CSS (Fixed-ratio responsive layout)
* **Audio Engine:** Tone.js, Web Audio API (AudioWorklet for Phasor Clock)
* **State Management:** Zustand
* **Persistence:** IndexedDB (Dexie.js recommended)
* **Icons:** Lucide React

## 🚀 Future Roadmap

### Phase 1: Core Implementation (Current)
* シングルユニット（1台）での音源、A/Bシーケンサー、保存機能の実装。

### Phase 2: Advanced Features
* Matrix、Metroタブの実装と、複雑なリズム生成ロジックの統合。

### 🔮 Future Goal: Dual DFAM System (Important)
最終的なゴールとして、**2台のユニット（Unit A / Unit B）を単一のアプリ内で統合する**ことを目指しています。

* **Dual Unit Integration:** 2台を並列動作させ、レイヤーや交互再生を実現。
* **Cross Modulation:** Unit AのLFOでUnit Bのフィルターを開くなど、ユニットを跨いだ変調の実装。
* **Stereo Field:** 各ユニットのパンニングによる広がりある音像の生成。

> **Note for Developers/AI:**
> このロードマップを見越して、シンセサイザーエンジンやシーケンサーのコードはSingleton（単一）ではなく、**インスタンス化可能なクラス/モジュール設計**（`new DFAMUnit()`のように複製可能な構造）を採用してください。

## 📦 Getting Started

```bash
# Clone the repository
git clone [repository-url]

# Install dependencies
npm install

# Start development server
npm run dev
